# Copilot instructions (compatibility pointer — not the source of truth)

Canonical instructions: `AGENTS.md` — read it first and follow it.

Task-specific skills (Agent Skills spec): `.agents/skills/*/SKILL.md`.
Skim `docs/agent/skills-index.md`, then load only the relevant `SKILL.md`
file(s) by path; read `docs/agent/` references only when needed.

Do not duplicate project rules here. If this file conflicts with `AGENTS.md`,
`AGENTS.md` wins.
